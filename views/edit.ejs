<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Todo</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white flex justify-center items-center min-h-screen px-4">
  <form action="/edit/<%= todo._id %>" method="post" class="bg-zinc-800 p-6 rounded-xl shadow-lg w-full max-w-lg flex flex-col gap-4">
    <h1 class="text-2xl font-bold text-center mb-2">Edit Todo</h1>
    <input class="bg-zinc-700 px-3 py-2 rounded w-full placeholder:text-zinc-400" type="text" name="todotitle" value="<%= todo.todotitle %>" required />
    <textarea class="bg-zinc-700 px-3 py-2 rounded w-full h-32 resize-none placeholder:italic placeholder:text-zinc-400" name="todocontent" required><%= todo.todocontent %></textarea>
    <button class="bg-blue-600 hover:bg-blue-700 transition text-white py-2 rounded font-medium w-full">Update</button>
    <a href="/profile" class="text-sm text-center text-zinc-400 hover:text-zinc-200 transition mt-2">Cancel</a>
  </form>
</body>
</html>
