<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Create Account</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gradient-to-br from-zinc-900 via-zinc-800 to-zinc-900 flex items-center justify-center text-white px-4">

  <div class="w-full max-w-lg bg-white/5 backdrop-blur-md rounded-2xl shadow-2xl p-8 flex flex-col items-center border border-white/10">
    <h1 class="text-3xl font-bold text-center mb-2">Create a New Account</h1>
    <p class="text-zinc-300 text-center text-sm mb-6">Join us and start organizing your tasks efficiently 🚀</p>

    <form action="/register" method="post" class="w-full flex flex-col gap-4">
      <input
        required
        class="bg-zinc-700/70 px-4 py-2 rounded-md placeholder:text-zinc-400 focus:outline-none focus:ring-2 focus:ring-blue-500"
        type="text"
        name="username"
        placeholder="Enter Username"
      />
      <input
        required
        class="bg-zinc-700/70 px-4 py-2 rounded-md placeholder:text-zinc-400 focus:outline-none focus:ring-2 focus:ring-blue-500"
        type="text"
        name="name"
        placeholder="Enter Name"
      />
      <input
        required
        class="bg-zinc-700/70 px-4 py-2 rounded-md placeholder:text-zinc-400 focus:outline-none focus:ring-2 focus:ring-blue-500"
        type="email"
        name="email"
        placeholder="Enter Email"
      />
      <input
        required
        class="bg-zinc-700/70 px-4 py-2 rounded-md placeholder:text-zinc-400 focus:outline-none focus:ring-2 focus:ring-blue-500"
        type="number"
        name="age"
        placeholder="Enter Age"
      />
      <input
        required
        class="bg-zinc-700/70 px-4 py-2 rounded-md placeholder:text-zinc-400 focus:outline-none focus:ring-2 focus:ring-blue-500"
        type="password"
        name="password"
        placeholder="Enter Password"
      />
      <input
        type="submit"
        value="Sign Up"
        class="bg-blue-600 hover:bg-blue-700 transition-colors text-white py-2 rounded-md font-semibold cursor-pointer"
      />
    </form>

    <a
      class="text-blue-400 hover:text-blue-300 mt-6 text-sm font-medium transition"
      href="/login"
    >
      Already have an account? Login here
    </a>
  </div>

</body>
</html>
